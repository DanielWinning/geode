#!/usr/bin/bash
snippets="/usr/local/bin/geode/scripts/snippets";

# Load helper functions and global variables
. "$snippets/functions.sh"

validateOperatingSystemOrExit

defineCommand help "help.sh" "Displays help information"
defineCommand server:setup "server/setup.sh" "Creates a new sudo user and runs initial server setup.\r\Arguments: username"

isValidCommand "$1" || errorAndExit "Invalid command: $1"

runCommandWithArgs "$@"

# Initialize variables for arguments and flags
#command_name=""
#declare -a flags
#
## Parse command-line arguments
#while (( "$#" )); do
#  case "$1" in
#    -*) # flags
#      flags+=("$1")
#      shift
#      ;;
#    *) # command name
#      command_name="$1"
#      shift
#      ;;
#  esac
#done
#
#if ! command -v node &> /dev/null
#then
#  writeErrorText "Node.js is not installed"
#  exit
#fi
#
#echo "Command name: $command_name"
#echo "Flags: " "${flags[@]}"